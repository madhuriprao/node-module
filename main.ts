import { countChar, reverseString } from "./stringUtilites"; // Remove the .ts extension

console.log(reverseString('Checking the Reverse string custom modules'));
console.log(countChar(' Learning custome modules '))

