import { countChar, reverseString, verticalText} from "./stringUtilites"; // Remove the .ts extension

console.log(reverseString('Learning Node.js is important because it enables developers to build efficient server-side applications, like using modules to reverse strings, count characters, and display text vertically, enhancing both performance and productivity.'));
console.log(countChar(' Learning Node.js is important because it enables developers to build efficient server-side applications, like using modules to reverse strings, count characters, and display text vertically, enhancing both performance and productivity. '));
verticalText('Vertical text');
